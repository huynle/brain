# OpenCode Plugins

Brain API integration plugins for OpenCode.

## Files

- `brain.ts` - Main brain API client with tools for save, recall, search, tasks
- `brain-planning.ts` - Planning enforcement with phase-based workflow

## Installation

Installed automatically via `brain install opencode` command.

Target location: `~/.config/opencode/plugins/`
