// Package frontmatter provides YAML frontmatter parsing for markdown files.
// This is a reusable package that can be imported by external projects.
package frontmatter

// TODO: Implement YAML frontmatter parser
// - Parse YAML between --- delimiters
// - Return structured frontmatter + remaining body
// - Support all brain entry frontmatter fields
